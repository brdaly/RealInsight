version: 2

updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:00"
      timezone: America/Detroit
    open-pull-requests-limit: 5
    groups:
      routine-npm-updates:
        patterns:
          - "*"
        exclude-patterns:
          - "@types/node"
        update-types:
          - minor
          - patch
      runtime-types:
        patterns:
          - "@types/node"
        update-types:
          - minor
          - patch
    ignore:
      # @types/node major versions require deliberate Node runtime upgrade.
      # Keep on Node 22.x until runtime is intentionally upgraded.
      # When upgrading Node, update engines, CI environment, and CHANGELOG together.
      - dependency-name: "@types/node"
        update-types:
          - version-update:semver-major

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:15"
      timezone: America/Detroit
    open-pull-requests-limit: 5
    groups:
      routine-action-updates:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
